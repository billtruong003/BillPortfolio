* text=auto eol=lf

# Unity WebGL payloads are served from Cloudflare R2, not tracked here.
# (The old zeno LFS rule pointed at a file that no longer exists in the repo.)
*.pdf binary
*.png binary
*.jpg binary
*.webp binary
*.glb binary
